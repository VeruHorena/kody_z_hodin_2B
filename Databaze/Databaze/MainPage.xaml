<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiDbApp.MainPage"
             Title="Seznam osob">
    <StackLayout Padding="10">
        <ListView x:Name="osobyList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Jmeno}" Detail="{Binding Prijmeni}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>